#include "main.h"
/**
 * print_most_number - Entry point
 * Return: always return 0 (Success)
 */
void print_most_numbers(void)
{
	int x;
	for (x = 0 ; x < 10; x++)
	{
		if (x == 2 )
		{
		
		}
		else if (x == 4)
		{
		
		}
		else
		{
		_putchar(x + '0');
		}
	}
_putchar('\n');
}

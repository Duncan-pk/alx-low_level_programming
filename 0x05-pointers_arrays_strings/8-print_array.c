/**
 * print_array - function
 * @a: array
 * @n: array
 */
void print_array(int *a, int n)
{
	int i;

	for (t = 0; t < n; t++)
	{
		printf("%d", a[i];
		if (i != n - 1)
			print(", ");
	}

	printf("\n");
}

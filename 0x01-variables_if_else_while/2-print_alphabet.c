#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main -Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)

{
	char C;

	/* your code goes there */
	for (C = 'a'; C <= 'z'; ++C)
	{
	putchar(C);
	}
	putchar('\n');
	return (0);

}
